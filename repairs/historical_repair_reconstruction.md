# 历史会话修复重建（待原件核验）

**状态：`unverified_reconstruction`。**以下为历史对话中已报告的修复主题；原始版本、逐条差异和回归审计尚未恢复。

## 已报告的修复主题

1. D12安全语言：安全检测器曾将拒答中被引用的行动词误判为行动指令；修复方向为在同句同时要求设备导向命令性语境，且不存在禁止/拒答语境。
2. D05/D07模板集中：后续生成要求增加独立答案结构、事实模式、替代解释、验证路径和回答顺序，不能以变更数字替代语义多样性。
3. D08生成器：曾发现蓝图架构索引越界；修复方向为明确蓝图配对、生成前断言和失败记录保留。
4. D06审计叙述：曾发现审计文字与计算字段不一致；修复方向为保留计算结果为准、记录叙述勘误且不放宽状态。
5. D02 BSF：曾发现生成器遗漏BSF公式中的平方根，导致派生值错误；修复方向为保留原始失败分片，修正公式并进行独立回归。

## 恒定规则

修复不得覆盖原始文件；必须保留失败审计、根因、修复版本、内容哈希和回归审计。恢复原始文件后，本重建内容必须以原始证据为准更新或撤销。